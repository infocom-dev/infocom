FROM node:12.2.0 as build
WORKDIR /app
ENV PATH /app/node_modules/.bin:$PATH
COPY package.json /app/package.json
RUN npm ci --only=production
RUN npm install -g @vue/cli
COPY . /app
RUN ["chmod", "+x", "./docker/frontend_setup.sh"]
CMD npm run build